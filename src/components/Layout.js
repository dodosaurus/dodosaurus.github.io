import React from 'react'

const Layout = ( {children} ) => {
    return (
        <main>
            <nav>

            </nav>
                { children }
            <footer>

            </footer>
        </main>
    )
};

export default Layout

